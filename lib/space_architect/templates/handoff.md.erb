# HANDOFF — <%= @_title %>

> Cross-slice table of contents for the Architect Loop. Per-slice detail lives in
> artifacts/<NN>-<slice>.md — this file only indexes the slices and carries
> mission-wide state. Keep it short (~150 lines): the next session must grok it
> in under a minute. Not in the committed artifacts = didn't happen.

## TL;DR (keep current)

- Goal: _[one sentence]_
- Last slice: _[<NN>-name — CONTINUE / KILL / awaiting verdict]_
- Next action: _[exact command or decision needed]_

## Repos in scope

| Repo | Path |
|------|------|
<% @_repos.each do |repo| -%>
| <%= repo["full_name"] || repo["name"] %> | <%= repo["path"] || "" %> |
<% end -%>
<% if @_repos.empty? -%>
| _(none yet — add repos with `space repo add`)_ | |
<% end -%>

## Verification gate (exact commands, per repo)

```
[install / test / lint / typecheck / build commands for each repo in scope]
```

## Slice index

<!-- Add a row per slice. Create slices with `space architect new <name>`. -->

| NN | Slice | Status | freeze_sha | Integration branch | Verdict | File |
|----|-------|--------|-----------|--------------------|---------|------|

Status values: speccing → frozen → dispatched → in-flight → awaiting-verdict → done.

## Open items for the human / architect

<!-- Blocking items: unresolved disagreements (which slice), scope questions,
stop-condition checkpoints. Detail lives in the slice file; link it. -->

## Decisions log (architect + human)

| Date | Decision | Why |
|------|----------|-----|
